{
  "_comment": "User-specific configuration mapping. Only applies if no role from roles.json matches. Order is irrelevant – matching is based on the exact username.",
  "users": [
    {
      "usernames": [],
      "config_path": ""
    }
  ]
}